function arrayPlusArray(arr1, arr2) {
  let newArr = [];
  arr1.forEach((arr2) => {
    arr2.forEach(() => {
      let sum = arr1[i] + arr2[i];
      return sum;
    });
    return sum;
  });
  return sum; //something went wrong
}
